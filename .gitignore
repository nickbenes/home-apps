node_modules
coverage

# Claude Code local settings (machine-specific permissions)
*.local.json
.claude/settings.local.json

# Ignore local data
data

# Seeder contains personal financial data (accounts, debts, etc.)
# See seed.example.ts for the version-controlled template
projects/benes-finance/backend/seed.ts